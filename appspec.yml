version: 0.0
os: linux

files:
   - source: /
     destination: /home/ec2-user/api

hooks:
  AfterInstall:
    - location: migrate.sh
      timeline: 300
  ApplicationStart:
    - location: npm-start.sh
      timeout: 60